{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "jshint src && jscs src && pulp build -- --censor-lib --strict",
    "test": "pulp test",
    "example": "pulp browserify -I example/src --to example/example.js"
  },
  "devDependencies": {
    "jscs": "^3.0.7",
    "jshint": "^2.9.4",
    "pulp": "^11.0.0",
    "purescript": "^0.11.4",
    "purescript-psa": "^0.5.0",
    "rimraf": "^2.5.4"
  }
}
